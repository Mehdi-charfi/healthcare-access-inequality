# App runtime — exactly what the deployed Streamlit dashboard imports.
# Kept minimal so Streamlit Community Cloud builds fast and never pulls the
# heavy geo/GDAL stack (geopandas, shapely…) that only the notebooks + the
# data-precompute step need. Those live in requirements-dev.txt.
streamlit>=1.33
pandas>=2.2
plotly>=5.24          # choropleth_map (maplibre renderer) needs >=5.24
